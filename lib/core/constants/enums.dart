enum UserType {
  customer,
  seller,
  admin,
}

enum GearBox{
  automaic,
  manual,
}

enum FuelType{
  petrol,
  electric,
  hybrid,
  naturalGas
}

enum RentalStatus{
  pending,
  approved,
  active,
  completed,
  cancelled,
}